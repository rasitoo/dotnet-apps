<Window x:Class="P06_01_DI_Contactos_TAPIADOR_rodrigo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:P06_01_DI_Contactos_TAPIADOR_rodrigo"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:P06_01_DI_Contactos_TAPIADOR_rodrigo.UI.ViewModels"
        xmlns:c="clr-namespace:P06_01_DI_Contactos_TAPIADOR_rodrigo.UI.Controls"
        xmlns:p="clr-namespace:P06_01_DI_Contactos_TAPIADOR_rodrigo.Properties"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <c:ButtonNav Text="{x:Static p:Resources.NavHome}" Icon="Home" IsChecked="True" Command="{Binding ActivateHomeViewCommand}" GroupName="SideMenu" />
                <c:ButtonNav Text="{x:Static p:Resources.NavProducts}" Icon="Library" Command="{Binding ActivateProductViewCommand}" GroupName="SideMenu" />
                <c:ButtonNav Text="{x:Static p:Resources.NavCategories}" Icon="Library" Command="{Binding ActivateCategoryViewCommand}" GroupName="SideMenu" />
                <c:ButtonNav Text="{x:Static p:Resources.NavSettings}" Icon="Settings" Command="{Binding ActivateSettingsViewCommand}" GroupName="SideMenu" />
            </StackPanel>
            <StackPanel Grid.Row="1">
            <c:ButtonNav Text="{x:Static p:Resources.NavExit}" Icon="ArrowExit" Command="{Binding ExitCommand}" />
            </StackPanel>

            <ContentControl Name="Content" Content="{Binding ActiveView}" Grid.Column="1" Grid.RowSpan="2" />
        </Grid>
</Window>
